# <@(#)tag:csongor.greyshirt.net,2017-08-11:tw/18.52.22z/10c187d>

#: FUNCTION ^G(no arguments)^g
#:   checks the column the cursor is in, and if it isn't in the first
#:   column, emits a reverse video percent (%) and a newline.
#:   Useful mostly in prompts.

function forceline {
	local IFS=' '
	set -- $($HOME/local/bin/get-row-col)
	(($2==1))|| print -- '\033[7m%\033[0m' >/dev/tty
  }

# vim: ft=ksh ts=4
